<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <!-- Usa el MISMO nombre que en JpaUtil -->
    <persistence-unit name="default" transaction-type="RESOURCE_LOCAL">
        <!-- Lista tus entidades -->
        <class>sv.edu.udb.www.desafio2.entities.Author</class>
        <class>sv.edu.udb.www.desafio2.entities.LiteraryGenre</class>

        <properties>
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/authors?useSSL=false&amp;serverTimezone=UTC"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="1234"/>

            <property name="eclipselink.logging.level" value="FINE"/>
            <property name="eclipselink.target-database" value="MySQL"/>

            <!-- Evita recrear la FK cada vez -->
            <property name="eclipselink.ddl-generation" value="none"/>

            <!-- Acepta EAGER y quita el warning -->
            <property name="eclipselink.weaving" value="false"/>
        </properties>
    </persistence-unit>
</persistence>